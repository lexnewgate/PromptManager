<Window x:Class="PromptManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:PromptManager"
        mc:Ignorable="d"
        Title="卡片管理" Height="600" Width="1000"
        MinHeight="400" MinWidth="600"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        FontFamily="{materialDesign:MaterialDesignFont}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 顶部标题栏 -->
        <materialDesign:ColorZone Grid.Row="0" Mode="PrimaryMid" Padding="16"
                                  materialDesign:ElevationAssist.Elevation="Dp4">
            <TextBlock Text="Prompt Manager" FontSize="24" VerticalAlignment="Center" />
        </materialDesign:ColorZone>

        <!-- 工具栏：保存、另存为 -->
        <Border Grid.Row="1" Padding="16,10" Background="{DynamicResource MaterialDesignPaper}"
                BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="OpenFileButton"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        ToolTip="打开已保存的卡片文件"
                        Margin="0,0,12,0"
                        Click="OpenFileButton_OnClick">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="FolderOpen" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,6,0" />
                        <TextBlock Text="打开" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
                <Button Command="{Binding SaveToDiskCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        ToolTip="保存到默认位置"
                        Margin="0,0,12,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ContentSave" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,6,0" />
                        <TextBlock Text="保存" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
                <Button x:Name="SaveAsButton"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        ToolTip="另存为到指定位置"
                        Margin="0,0,12,0"
                        Click="SaveAsButton_OnClick">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ContentSaveAll" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,6,0" />
                        <TextBlock Text="另存为" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
                <Button x:Name="NewCardButton"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        ToolTip="新建卡片"
                        Click="NewCardButton_OnClick">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Plus" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,6,0" />
                        <TextBlock Text="新建卡片" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- 卡片列表 -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="16">
            <ItemsControl x:Name="CardsItemsControl"
                          ItemsSource="{Binding Cards}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Width="220" Height="140" Margin="8"
                                             Cursor="Hand"
                                             RenderTransformOrigin="0.5,0.5"
                                             materialDesign:ElevationAssist.Elevation="Dp2"
                                             MouseDoubleClick="Card_MouseDoubleClick">
                            <materialDesign:Card.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1" />
                            </materialDesign:Card.RenderTransform>
                            <materialDesign:Card.Style>
                                <Style TargetType="materialDesign:Card" BasedOn="{StaticResource MaterialDesignOutlinedCard}">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp8" />
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.02" Duration="0:0:0.2">
                                                            <DoubleAnimation.EasingFunction>
                                                                <QuadraticEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.02" Duration="0:0:0.2">
                                                            <DoubleAnimation.EasingFunction>
                                                                <QuadraticEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.2">
                                                            <DoubleAnimation.EasingFunction>
                                                                <QuadraticEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.2">
                                                            <DoubleAnimation.EasingFunction>
                                                                <QuadraticEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </materialDesign:Card.Style>
                            <Grid Margin="16">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis" MaxHeight="24" />
                                <TextBlock Grid.Row="1" Text="{Binding Summary}" Margin="0,8,0,0"
                                           FontSize="13" Opacity="0.8" TextWrapping="Wrap"
                                           TextTrimming="CharacterEllipsis" MaxHeight="60" />
                            </Grid>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- 底部状态栏：当前读取的文件 -->
        <Border Grid.Row="3" Padding="16,8" Background="{DynamicResource MaterialDesignPaper}"
                BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,1,0,0">
            <TextBlock Text="{Binding CurrentFilePath, StringFormat='当前文件：{0}'}"
                       FontSize="12" Opacity="0.7" VerticalAlignment="Center"
                       TextTrimming="CharacterEllipsis" ToolTip="{Binding CurrentFilePath}" />
        </Border>
    </Grid>
</Window>
